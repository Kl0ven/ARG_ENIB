<div class="arg-card">
	<h1>{{name}}</h1>

	<p>
		{{enigma_text}}
	</p>
	{{#if hint}}
		{{> hint}}
	{{/if}}

	<div class="py-5 d-flex justify-content-around flex-wrap"  id="fill" align="center">

	</div>

	{{> form_flag}}

	{{> modal}}
	{{> discord}}
</div>
